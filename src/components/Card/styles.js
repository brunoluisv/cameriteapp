import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  titulo: {
    textAlign: 'center',
    marginBottom: 6,
    fontSize: 18,
  },
  container: {
    height: 180,
    width: '100%',
    borderWidth: 1,
    borderColor: '#CBCBCB',
    backgroundColor: '#000',
    borderRadius: 5,
    marginBottom: 10
  },
  thumb: {
    width: '100%',
    height: '100%',
  }
});